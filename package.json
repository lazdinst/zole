{
  "private": true,
  "name": "zole-monorepo",
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "build:shared": "cd shared && npm run build",
    "socket:client": "cd server && ./run-socket-test.sh"
  }
}
